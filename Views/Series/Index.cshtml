@model IEnumerable<Filmera.Models.Serie>

﻿@{
    ViewData["Title"] = "Series registradas";
    var msg = TempData["Mensaje"] as string;
}

<h1>@ViewData["Title"]</h1>

@if (!string.IsNullOrEmpty(msg))
{
    <div class="alert alert-info" role="alert">
        @msg
    </div>
}

<form asp-action="BuscarSer" method="post" class="row g-2">
    <div class="col-9">
        <input id="titulo" name="titulo" type="text" class="form-control" placeholder="Nombre de la serie" />
    </div>
    <div class="col-3 d-grid">
        <button type="submit" class="btn btn-primary"><i class="bi bi-search me-2"></i>Buscar</button>
    </div>

    <a class="btn btn-primary" asp-controller="Series" asp-action="Index">
        <i class="bi bi-person-plus me-2"></i>Mostrar todas
    </a>
</form>

<p>
    <a class="btn btn-primary" asp-controller="Series" asp-action="Create">
        <i class="bi bi-person-plus me-2"></i>Añadir Serie
    </a>
</p>

@if (!Model.Any())
{
    <p>No hay series registradas aun</p>
}
else
{
    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>Codigo Serie</th>
                <th>Titulo</th>
                <th>Sinopsis</th>
                <th>Genero</th>
                <th>Temporadas</th>
                <th>Episodios</th>
                <th>Calificacion (0 - 100)</th>
                <th>Lanzamiento (año)</th>
                <th>Publico</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var p in Model)
            {
                <tr>
                    <td>@p.IdSerie</td>
                    <td>@p.Titulo</td>
                    <td>@p.Sinopsis</td>
                    <td>@p.Genero</td>
                    <td>@p.Temporadas</td>
                    <td>@p.Episodios</td>
                    <td>@p.Calificacion</td>
                    <td>@p.Lanzamiento</td>
                    <td>@p.Publico</td>
                </tr>
            }
        </tbody>
    </table>

    <br />
    <hr />
    <br />

    // Formulario para editar la información de una serie
    <h5 class="card-title">Actualizar serie</h5>
    <form asp-action="Edit" method="get" class="row g-2">
        <div class="col-8">
            <input id="EditarSerie" name="IdSerie" type="text" class="form-control" placeholder="Código identificador de serie" />
        </div>
        <div class="col-4 d-grid">
            <button type="submit" class="btn btn-outline-warning"><i class="bi bi-pencil me-2"></i>Actualizar</button>
        </div>
    </form>

    <br />
    <hr />
    <br />

    // Eliminar serie
    <h5 class="card-title">Eliminar serie</h5>
    <form asp-action="Delete" method="post" class="row g-2">
        <div class="col-8">
            <input id="EliminarSer" name="IdSerie" type="text" class="form-control" placeholder="Código identificador de serie" />
        </div>
        <div class="col-4 d-grid">
            <button type="submit" class="btn btn-outline-danger"><i class="bi bi-trash-fill me-2"></i>Eliminar</button>
        </div>
    </form>

    <br />
    <hr />
    <br />

    // Consultas Linq
    <h5 class="card-title">Filtros</h5>

    <div class="row g-2 mb-3">
        <form asp-action="FiltroGenero" method="post" class="col-md-6 d-flex">
            <select name="genero" class="form-select me-2">
                <option value="">Seleccione...</option>
                <option value="Acción">Acción</option>
                <option value="Terror">Terror</option>
                <option value="Romance">Romance</option>
                <option value="Suspenso">Suspenso</option>
                <option value="Comedia">Comedia</option>
                <option value="Fantasía">Fantasía</option>
                <option value="Animación">Animación</option>
                <option value="Drama">Drama</option>
            </select>
            <button type="submit" class="btn btn-secondary">Filtrar por género</button>
        </form>

        <form asp-action="FiltroClasificacion" method="post" class="col-md-6 d-flex">
            <select name="publico" class="form-select me-2">
                <option value="">Seleccione...</option>
                <option value="Y">TV-Y (Apto para niños pequeños)</option>
                <option value="Y7">TV-Y7 (Para 7 años en adelante)</option>
                <option value="G">TV-G (Todo público)</option>
                <option value="PG">TV-PG (Supervisión de padres recomendada)</option>
                <option value="TV-14">TV-14 (No apta para menores de 14)</option>
                <option value="MA">TV-MA (Solo adultos)</option>
            </select>
            <button type="submit" class="btn btn-secondary">Filtrar por público</button>
        </form>
    </div>

    <h5 class="card-title">Orden</h5>

    <div class="col-md-6 d-flex align-items-center">
        <a class="btn btn-secondary w-100" asp-action="OrderTitulo"><i class="bi bi-sort-alpha-down me-2"></i>Alfabético</a>
    </div>

    <div class="col-md-6 d-flex align-items-center">
        <a class="btn btn-secondary w-100" asp-action="OrderFecha"><i class="bi bi-sort-alpha-down me-2"></i>Más recientes</a>
    </div>

    <div class="col-md-6 d-flex align-items-center">
        <a class="btn btn-secondary w-100" asp-action="OrderRating"><i class="bi bi-sort-alpha-down me-2"></i>Mejor valoradas</a>
    </div>
}

